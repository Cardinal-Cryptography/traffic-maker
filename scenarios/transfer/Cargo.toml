[package]
name = "scenario-transfer"
version = "0.1.0"
edition = "2021"

[dependencies]
traffic = { path = "../../traffic" }
chain-support = { path = "../../chain-support" }
common = { path = "../../common" }

async-trait = "0.1"
log = "0.4"
